<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eda.mapper.AdminerMapper">
    <select id="selectAdminById" parameterType="int" resultType="string">
        select ad_username from adminer where ad_id=#{param1}
    </select>
</mapper>